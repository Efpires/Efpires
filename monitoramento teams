1. Delete o item "ICMP Jitter" atual
2. Crie novo item assim:

    Name: ICMP Jitter

    Type: Dependent item ✅ (não Calculated!)

    Key: icmp.jitter

    Master item: Selecione ICMP latency (o icmppingsec do seu host)

    Type of information: Numeric (float)

    Units: ms

3. Na aba Preprocessing, adicione:

Step 1:

    Type: JavaScript

    Parameters:

javascript
if (typeof prev_value === 'undefined') {
    return value;
}
return Math.abs(value - prev_value);

Step 2:

    Type: Simple change

    (deixe vazio)
